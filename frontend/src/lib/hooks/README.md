# Shared Hooks

This folder contains shared data retrieval functions used by 2 or more components.

If a hook is used by a single component or page, it should be moved closer to that component (e.g., in a local `hooks.ts` or refactored into `page.svelte.ts`).
