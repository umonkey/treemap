build:
	npm run build

install:
	npm install

lint:
	npm run lint

serve:
	REACT_APP_API_ROOT=http://localhost:8000 npm run dev

storybook:
	npm run storybook -- --no-open

test:
	npm run test

test-verbose:
	npm run test-verbose
