#!/bin/sh
if [ -z "$1" ]; then
    echo "Usage: $0 filename.mp4"
    exit 1
fi

mpv --hwdec=no --osd-msg1='Frame: ${estimated-frame-number} / ${estimated-frame-count}' --vf=lavfi='[drawgrid=w=iw/4:h=ih:x=iw*0.5:c=red:t=2]' "$1"
