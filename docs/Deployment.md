# Deployment

## Frontend

The frontend is currently just a folder of static files and is being served by Cloudflare Pages.  The files are deployed using the official Wrangler CLI, the workflow file is `.github/workflows/deploy-frontend.yml`.
